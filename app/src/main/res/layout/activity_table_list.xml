<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFBEA">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tablesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="12dp" />

    <Button
        android:id="@+id/btnDeleteTable"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:text="DELETE SELECTED TABLE"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:background="@drawable/rounded_button" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddTable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_margin="20dp"
        android:src="@drawable/ic_add"
        android:contentDescription="Add Table"
        app:backgroundTint="#DDB892"
        app:tint="@android:color/white" />

</LinearLayout>